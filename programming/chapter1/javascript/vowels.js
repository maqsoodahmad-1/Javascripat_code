"use strict"
let name = "Durgesh";
for(let i = 0 ; i <name.length ; i++)
{
  if(name[i] === 'a' ||name[i] === 'e' || name[i] === 'i' || name[i] === " o" ||name[i] === "u" )
  {
   console.log(name[i]);
  }
}

