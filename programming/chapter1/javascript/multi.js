function multiple(a,b)
{
    return a*b;
}
function addition(p,q){
    return p + q;
}

function division(m,n) {
    return m /n;
}

let x= prompt("Enter the number");
let y = prompt("Enter the anothe number");
alert(multiple(x,y));
alert(addition(x,y));
alert(division(x,y));