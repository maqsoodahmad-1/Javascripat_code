let x = prompt("Enter the first number");
let y = prompt("Enter the second number ");
let z = prompt("Enter the third number");
if(x > y ){
  if(x >z){
     alert(`${x} is greater of all the three`);
    } else {
     alert(`${z} is greater`);
    }
 }
 else {
   if(y > z )
  {
     alert(`${y} is greater of all`);
  } else {
     alert(`${z} is greater of all `);
  }
}

  

